/* Mat-form-field similar a tus inputs */
:host ::ng-deep .mat-form-field-appearance-outline {
  width: 100%;
  border-radius: 8px;
  border: 1px solid var(--bp-blue-experto);
  box-shadow: none;
  padding: 0;
}

/* Focus con shadow */
:host ::ng-deep .mat-form-field-appearance-outline.mat-focused {
  border-color: var(--bp-aqua-digital);
  box-shadow: 0 0 0 2px var(--bp-aqua-digital);
}

/* Hover del mat-select */
:host ::ng-deep .mat-select:hover .mat-select-trigger {
  background-color: #f0f8ff; /* azul muy suave */
  cursor: pointer;
}

/* Opciones: hover azul */
:host ::ng-deep .mat-mdc-option:hover {
  background-color: #007bff;
  color: white;
}

/* Opciones: tamaño y scroll */
:host ::ng-deep .mat-mdc-select-panel {
  max-height: 250px; /* limita altura y evita scroll si hay pocas opciones */
  font-size: 14px;
}








<mat-form-field appearance="outline" class="w-100">
  <mat-label>Área</mat-label>
  <input type="text" placeholder="Seleccione un área" aria-label="Área" matInput 
         [formControl]="areaCtrl" [matAutocomplete]="auto">

  <mat-autocomplete #auto="matAutocomplete">
    <mat-option *ngFor="let area of filteredAreas | async" [value]="area.nombre">
      {{ area.nombre }}
    </mat-option>
  </mat-autocomplete>

  <mat-error *ngIf="puestoTrabajoForm.get('areaId')?.touched && puestoTrabajoForm.get('areaId')?.invalid">
    Campo obligatorio
  </mat-error>
</mat-form-field>




import { Component } from '@angular/core';
import { FormControl } from '@angular/forms';
import { Observable, map, startWith } from 'rxjs';

@Component({
  selector: 'app-puesto-trabajo',
  standalone: true,
  imports: [
    ReactiveFormsModule,
    MatFormFieldModule,
    MatInputModule,
    MatAutocompleteModule
  ],
  templateUrl: './puesto-trabajo.component.html'
})
export class PuestoTrabajoComponent {
  areaCtrl = new FormControl('');
  areas = [
    { id: 1, nombre: 'Área 1' },
    { id: 2, nombre: 'Área 2' },
    { id: 3, nombre: 'Área 3' }
  ];

  filteredAreas: Observable<any[]>;

  constructor() {
    this.filteredAreas = this.areaCtrl.valueChanges.pipe(
      startWith(''),
      map(value => this._filter(value))
    );
  }

  private _filter(value: string): any[] {
    const filterValue = value.toLowerCase();
    return this.areas.filter(area => area.nombre.toLowerCase().includes(filterValue));
  }
}
