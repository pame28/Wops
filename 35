<div class="card-backdrop">
  <div class="card modal-card">

    <button class="close-card" (click)="onClose.emit()">&times;</button>

    <h1 class="titulo-modal">{{ title }}</h1>

    <form [formGroup]="form" (ngSubmit)="onSubmit.emit()">
      <ng-content></ng-content>

      <div class="modal-actions">
        <button class="btn-crear" type="submit">
          {{ submitLabel }}
        </button>

        <button class="btn-outline" type="button" (click)="onClose.emit()">
          Cancelar
        </button>
      </div>
    </form>

  </div>
</div>








.card-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 999;
}

.modal-card {
  width: 500px;
  padding: 50px;
  border-radius: 12px;
  box-shadow: 0 20px 50px rgba(0,0,0,.3);
  background: white;
  position: relative;
}

.close-card {
  position: absolute;
  top: 10px;
  right: 15px;
  font-size: 40px;
  background: transparent;
  border: none;
  cursor: pointer;
  color: red;
  font-weight: bolder;
}

.titulo-modal {
  color: #003865;
  font-family: "Boogaloo", sans-serif;
  text-align: center;
}

:host ::ng-deep label {
  font-weight: 600;
  color: var(--bp-blue-experto);
  display: block;
  margin-top: 15px;
}

:host ::ng-deep input {
  width: 100%;
  margin-top: 8px;
  padding: 8px;
  border-radius: 8px;
  border: 1px solid var(--bp-blue-experto);
}

:host ::ng-deep input:focus {
  border-color: var(--bp-aqua-digital);
  outline: none;
  box-shadow: 0 0 0 2px var(--bp-aqua-digital);
}

:host ::ng-deep .error {
  color: red;
  font-size: 0.9rem;
  margin-top: 6px;
}

.modal-actions {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  margin-top: 40px;
}

.btn-crear {
  background: #00C1D4;
  color: white;
  padding: 6px 14px;
  border-radius: 8px;
  border: none;
  width: 150px;
  font-weight: bolder;
}

.btn-outline {
  border: 1px solid #ccc;
  background: transparent;
  padding: 6px 14px;
  border-radius: 8px;
  width: 150px;
  font-weight: bolder;
}










<div class="content-wrapper">

  <div class="main-title">
    <div class="home-title">
      <h1>Accesos</h1>
    </div>
  </div>

  <div class="layout">

    <app-side-bar
      (sidebarToggled)="onSidebarToggled($event)">
    </app-side-bar>

    <div class="content" [class.collapsed]="isSidebarCollapsed">

      <app-crud-layout
        createLabel="Nuevo Acceso"
        [data]="accesos"
        [columns]="columns"
        (onCreate)="openCreateCard()"
        (onEdit)="openEditCard($event)"
        (onDelete)="deleteAcceso($event.id)">
      </app-crud-layout>

    </div>
  </div>
</div>

<app-crud-modal
  *ngIf="showCard"
  [title]="editMode ? 'Editar Acceso' : 'Nuevo Acceso'"
  [submitLabel]="editMode ? 'Actualizar' : 'Crear'"
  [form]="accesoForm"
  (onClose)="closeCard()"
  (onSubmit)="saveAcceso()">

  <label>Nombre Acceso</label>
  <input type="text" formControlName="nombre" />
  <div class="error" *ngIf="accesoForm.get('nombre')?.touched && accesoForm.get('nombre')?.invalid">
    Campo obligatorio
  </div>

  <label>Descripción Acceso</label>
  <input type="text" formControlName="descripcion" />
  <div class="error" *ngIf="accesoForm.get('descripcion')?.touched && accesoForm.get('descripcion')?.invalid">
    Campo obligatorio
  </div>

</app-crud-modal>





















.layout {
  display: flex;
  height: 100vh;
}

.content {
  flex: 1;
  padding: 40px;
  transition: margin-left .4s ease;
}

.content:not(.collapsed) {
  margin-left: 220px;
}

.main-title {
  display: flex;
  justify-content: center;
  margin-bottom: 20px;
}

.home-title h1 {
  font-size: 3rem;
  color: #003865;
  font-family: "Boogaloo", sans-serif;
}

.content-wrapper {
  flex-grow: 1;
  display: flex;
  flex-direction: column;
  overflow: auto;
}





